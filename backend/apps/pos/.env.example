VITE_API_BASE=http://localhost:9000
VITE_VENDOR_ID=v_demo_grocer
VITE_TERMINAL_ID=term_dev_1

# DEV-ONLY admin auth shim. Reads these creds at startup, exchanges them for a
# Medusa admin JWT once, memoizes. The PoS calls /admin/* routes and needs auth.
# Production model is per-vendor scoped tokens via a real onboarding flow (H-3.1).
# Leave UNSET in any environment where this would be a security risk.
VITE_DEV_ADMIN_EMAIL=admin@hanoot.iq
VITE_DEV_ADMIN_PASSWORD=hanoot123
