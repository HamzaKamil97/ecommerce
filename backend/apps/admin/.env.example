VITE_API_BASE=http://localhost:9000
# Vendor whose reports the dashboard shows. The login screen allows override,
# but the form prefills from this value to save typing in dev.
VITE_VENDOR_ID=v_demo_grocer
